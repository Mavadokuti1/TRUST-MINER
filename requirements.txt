# Trust-Miner dependencies
# Uses minimum-version floors (>=) instead of exact pins to avoid
# conflicting with other system-wide packages (hermes-agent, litellm, etc.)

requests>=2.32.2
python-frontmatter>=1.1.0
python-telegram-bot>=21.3,<23.0
python-dotenv>=1.0.1
aiohttp>=3.10.0
praw>=7.7.1
spintax>=1.0.4
linkitin>=0.1.3
medium-ops
